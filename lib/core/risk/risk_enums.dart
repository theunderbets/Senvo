enum RiskLevel { unknown, low, moderate, elevated, high, critical }

enum HealthRiskLevel {
  normal,
  watch,
  alert,
  emergency,
}

enum RiskFactorSeverity { info, moderate, high, severe }

enum TrendDirection { rising, stable, falling }

enum DataFreshness { fresh, recent, stale, expired, unavailable }
